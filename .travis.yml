language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r simple-application *
  - mkdir -p deploy
  - mv simple-application.zip deploy/simple-application.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # Travis repo setting
    secret_access_key: $AWS_SECRET_KEY # Travis repo setting
    bucket: kys-springboot2-webservice
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy # before_deploy에서 생성한 디렉토리
    wait-until-deployed: true

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # Travis repo setting
    secret_access_key: $AWS_SECRET_KEY # Travis repo setting
    bucket: kys-springboot2-webservice
    key: simple-application.zip
    bundle_type: zip
    application: kys-springboot2-webservice # 웹콘솔에 등록한 CodeDeploy 어플리케이션
    deployment_group: kys-springboot2-webservice-group # 웹콘솔에 등록한 코드 deploy 그룹
    region: ap-northeast-2
    wait-until-deployed: true

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - dydtjd0245@gmail.com